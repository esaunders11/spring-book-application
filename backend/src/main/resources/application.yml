spring:
  application:
    name: library
  profiles:
    active: prod
  datasource:
    url: jdbc:mysql://localhost:3306/library_api?createDatabaseIfNotExist=true
    username: root
    password: 12226578
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

